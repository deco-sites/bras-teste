
/**
* The code snippet below is an example.
*/

/**
* import { integer, sqliteTable, text } from "drizzle-orm/sqlite-core";

export const profiles = sqliteTable("profiles", {
  id: integer("col1").primaryKey({ autoIncrement: true }),
  name: text("name"),
  email: text("email"),
});
*/
